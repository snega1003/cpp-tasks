gtest = dependency('gtest', main:true)
test('rotate_vector',executable('rotate_test',
                sources: ['rotate_test.cc', '../project/main.cc'],
                include_directories: src,
                dependencies: [gtest]
    )
    
)
