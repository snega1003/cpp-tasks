gtest = dependency('gtest', main:true)
test('Trait',executable('trait_test',
                sources: ['trait_test.cc'],
                include_directories: src,
                dependencies: [gtest]
    )
    
)
